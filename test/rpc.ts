describe("RPC", () => {
  it("Should be able to send message in a RPC fashion")
  it("Should be able to automatically reply to an rpc session")
  it("Should be able to use an rpc call as a function that returns a promise")
  it("Should be able to use an rpc call as a function that has a callback")
})